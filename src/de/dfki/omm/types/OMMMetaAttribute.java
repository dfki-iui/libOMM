package de.dfki.omm.types;

/** Enumerator listing all metadata fields of OMM blocks. */
public enum OMMMetaAttribute
{
	ID, Creator, Contributor, Type, Format, Namespace, Link, Payload, Subject, Title, Description
}
